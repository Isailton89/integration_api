package com.integration.api.integration;

public record RecordDataIntegration(
        String id,
        String dateRegister,
        String contact,
        String status,
        String halfFinisher,
        String error,
        String responsible) {
}
